import React from 'react';
import {store} from "../../../shared/lib/store/store";

export const Orders = () => {
    const user = store.getState().user;
    return (
        <div>

        </div>
    );
};

